import React from 'react';
// import { useSelector } from 'react-redux';

import '../styles/components/common/_subscribe-cta.scss';

const SubscribeCallToAction = () => {
  // const fighters = useSelector(state => state.fighters.fighters);
  // const [activeFighter, setActiveFighter] = useState();
  // const [videosToShow, setVideosToShow] = useState([]);

  // useEffect(() => {
  //   setActiveFighter(fighters[0]?.id);
  // }, [fighters]);

  return <div className="subscribe-cta">Subscribe!!</div>;
};

export default SubscribeCallToAction;
